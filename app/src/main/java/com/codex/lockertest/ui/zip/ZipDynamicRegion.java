package com.codex.lockertest.ui.zip;

/** Areas whose content remains native and live above a ZIP visual shell. */
public enum ZipDynamicRegion {
    NONE,
    HOME_INPUTS,
    CREDENTIAL_STATUS,
    FACE_PREVIEW,
    LOCKER_GRID,
    RESULT_MESSAGE,
    RETURN_AUTH,
    RETURN_LIST,
    RETURN_PROGRESS,
    ADMIN_PIN,
    ADMIN_PANEL,
    SERIAL_PANEL,
    SDK_PANEL,
    ENROLLMENT_PANEL
}
