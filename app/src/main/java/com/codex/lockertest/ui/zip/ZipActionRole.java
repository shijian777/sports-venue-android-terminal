package com.codex.lockertest.ui.zip;

/** Semantic roles that a visible ZIP-screen control can invoke. */
public enum ZipActionRole {
    NONE,
    BACK,
    HOME,
    RETRY,
    CONFIRM,
    CANCEL,
    SELECT,
    OPEN,
    RETURN_LOCKER,
    FACE,
    PALM,
    ENROLL,
    SERIAL_ADMIN,
    FACE_SDK_ADMIN,
    SERIAL_OPEN,
    SERIAL_CLOSE,
    SERIAL_TEST,
    SDK_ACTIVATE,
    SDK_INITIALIZE,
    LIVENESS_TOGGLE
}
