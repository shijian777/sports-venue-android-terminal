# 源码恢复与配置说明

本仓库是 2026-09-12 从当前 `smart-locker-serial-test-v17` 工作目录导出的私有源码快照，包含尚未提交到原工程 Git 历史的最新文件。目录名中的 v17 不表示当前 APK 版本；现有 Gradle 配置为 v21 前端集成测试版。

## 保留的内容

- `app/`：Android 业务逻辑、设备接入、网络接口、界面、字体、原始页面素材及测试。
- `app/libs/` 和模型、原生库：原工程已经使用的百度人脸和掌静脉等依赖；相关权利属于各自厂商。私有仓库备份不代表获得公开分发或免激活的权利。
- `scripts/`、`tools/`、`docs/`：现有构建、审计、回归工具及项目说明。
- `SOURCE-SNAPSHOT.sha256`：本次初始快照文件的 SHA-256 清单，不包含清单自身。后续修改后它不会自动更新。

## 未上传的内容

- 真实服务器测试签名凭据提供器 `app/src/production/java/com/codex/lockertest/server/ProductionSecretProvider.java`。
- APK、签名证书和私钥、`local.properties`、临时日志、测试现场数据、构建输出、缓存、旧 Git 历史。
- 原工作区中工程外的历史 APK、冻结基线、旧版本源码和 Android/JDK 安装目录。

原电脑的工作工程和真实凭据文件未被修改或删除。

## 换电脑后如何恢复

1. 克隆此私有仓库。安装兼容本工程 Gradle/Android 插件的 JDK、Android SDK，并在本机配置 SDK 路径。仓库使用 Android Gradle Plugin 8.9.2，未随附 Gradle wrapper；不要假定执行 `gradlew` 即可构建。
2. 需要构建 `production` 时，从可信的原工程或经授权的凭据管理渠道恢复上述 `ProductionSecretProvider.java`。该路径已加入 `.gitignore`，不要强制提交。旁边的 `.java.example` 仅说明接口形状，返回空凭据，不会放行联网业务。
3. 百度在线激活、终端备案、测试会员数据和服务端权限仍需按原有流程完成。仓库备份不会生成或替代这些授权。
4. `scripts/build-debug.ps1` 原样保留了原电脑工具路径、历史版本保护检查、冻结文件和交付目录约束。新电脑缺少这些输入时该脚本会失败；应先恢复相应依赖与基线，再经验证调整路径，不能通过删除安全审计来假装构建成功。
5. `ProductionSecretProviderTest` 等现有测试包含原测试凭据对应的固定签名断言。未恢复对应凭据，或更换凭据后，相关断言不会通过；应使用经确认的测试向量复验，而非绕过鉴权。

## 验证范围

此次上传验证源码复制完整性、排除项、提交与远端一致性，不代表重新完成 APK 构建、服务器联调或真机验收。私有快照的配置边界与原电脑的可运行工程不同，不应直接当作正式发布包。

后续正常开发可以在此仓库中提交更改。凭据、证书和现场个人数据继续保留在受控本地配置或专门的密钥管理渠道，不要写入 Git 历史。
