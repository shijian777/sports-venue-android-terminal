# 最终资料包对齐

授权依据：用户 2026-09-06 明确指定 `智能更衣柜app(2).zip` 为最终要求，并要求先完成可以实现的部分，剩余问题最后集中说明。无需再次逐条审批设计。

## 本轮设计

沿用现有 Android 原生界面、1280×800 乾卦布局、进程级串口所有权和百度检测组件。分子项目逐项实现：基础配置协议；身份与业务接口；配置呈现与激活；可验证业务界面。每一项需要行为测试、代码复核和构建证据。生产失败不能退回本地模拟认证。

最终资料优先于此前聊天约定，但文档内部冲突不得凭猜测驱动物理开柜。普通用柜流程无需向服务器上传锁板回包，不等于客户端可以把发送失败显示为开柜成功。接口示例的单柜 `00` 与硬件文档的全开 `00` 冲突，不能作为单柜命令执行。MQTT 连接资料、文件上传签名约定、掌纹 CPU 兼容性等必须记录实际缺项，不虚构参数。

## 子项目一：基础配置协议

1. 按新版 `baseSetting` 支持数字 `locker_check_status`，兼容旧版字符串 0/1/2。
2. 新增可选的 `activation_code` 和 `voice`。激活码可为空；非空值只做有限长度、控制字符检查，不写日志、不自动在线激活。语音按示例为文本到 URL 的对象；为空数组时按空配置兼容。非空数组不猜含义。
3. 使用/还柜须知允许换行、回车、制表符；禁止其他控制字符。标题、URL、标识符等仍保持单行。
4. 基础数据中的动态验证开关兼容文档表格的数字和示例的字符串 0/1；设备号兼容字符串及规范非负整数，保持最终请求头为字符串，不改变字符串前导零。
5. 新字段不能使旧的测试数据、SDK 模型或锁板协议发生变化。

## 验收

新版脱敏完整响应能解析；旧版响应仍能解析；未知/非法状态、重复键、控制字符、错误结构仍被拒绝；不泄露激活码；原有测试中旧契约断言仅在明确被最终文档取代时调整；不删除安全测试。

## 本轮禁止的外部副作用

不实际打开柜门，不向服务器创建/归还业务记录，不消耗百度激活次数，不上传真实人脸，不改系统配置。不覆盖已交付原 APK。真机和服务器业务联调未做时必须明确说明。
